pipeline
{
tools{
    maven 'maven'
   

}
    agent {

    label 'node1'

     }
    stages{
        stage('build'){
            steps{
                sh 'mvn clean package'
                archiveArtifacts artifacts: '**/target/*.jar'
            }
        }
    }
}
